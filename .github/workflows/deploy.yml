name: CI/CD - Deploy to Railway

on:
  push:
    branches:
      - deployment-test  # Trigger on push to the deployment branch

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    environment: Springboot_Backend_deployment

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Java 21
        uses: actions/setup-java@v3
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Make Maven wrapper executable
        run: chmod +x mvnw

      - name: Build JAR
        run: ./mvnw clean package -DskipTests

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

#      - name: Install Railway CLI
#        run: npm install -g @railway/cli
#
#      - name: Deploy to Railway
#        env:
#          RAILWAY_TOKEN: ${{ secrets.RAILWAY_API_KEY }}   # <-- token here
#        run: railway up --service hrms-api --detach --yes # --yes skips prompts
